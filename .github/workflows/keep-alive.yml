name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 6 * * 1'   # Varje måndag kl 06:00 UTC (ca 07-08 svensk tid)
  workflow_dispatch:        # Går även att köra manuellt via "Run workflow"-knappen

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://zeixqdprasspwpbvcgbl.supabase.co/rest/v1/bookings?select=key&limit=1" \
            -H "apikey: sb_publishable_jrJXiwRRIgCNdR0YNFO4JQ_ET_n6J3P" \
            -H "Authorization: Bearer sb_publishable_jrJXiwRRIgCNdR0YNFO4JQ_ET_n6J3P"
